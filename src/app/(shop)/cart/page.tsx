import { CartContent } from "@/app/_components/shop/CartContent";

/*
|--------------------------------------------------------------------------
| مسیر فایل: src/app/(shop)/cart/page.tsx
|--------------------------------------------------------------------------
*/

export const metadata = {
  title: "سبد خرید | یدکی",
};

export default function CartPage() {
  return <CartContent />;
}
